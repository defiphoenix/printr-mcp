bunx --no -- commitlint --edit "$1"

# Reject commits with Co-Authored-By trailers
if grep -qi "^Co-Authored-By:" "$1"; then
  echo "Error: Co-Authored-By trailers are not allowed in commit messages."
  exit 1
fi
